# docker
    docker-compose up
    
# url
    localhost:3000
    config lib/database.js, update db url if db running at localhost
# ubuntu
    npm install
    npm run build
    npm run start

# login account
    root_user
    s47_secret_pass
